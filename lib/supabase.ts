// supabase.ts — Supabase clients (Step 5 wires into generate-pdf / submit)
//
// TODO(Step 5):
// - Client-side instance (browser): createBrowserClient-style client using
//   NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY. Must never
//   reference SUPABASE_SERVICE_ROLE_KEY.
// - Server-side instance (API routes): admin client using
//   SUPABASE_SERVICE_ROLE_KEY — server-only, never imported in client code.
// - Storage bucket: "patient-pdfs" (create/verify in Supabase dashboard
//   during Step 5).
export {}; // scaffold — implemented in Step 5
