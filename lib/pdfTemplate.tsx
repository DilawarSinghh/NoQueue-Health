// pdfTemplate.tsx — @react-pdf/renderer template (Step 5)
//
// TODO(Step 5):
// - Export an IntakePdfDocument component using @react-pdf/renderer's
//   Document / Page / Text / View primitives (NOT HTML):
//   * Clinic name / letterhead style header (clinic name constant).
//   * Each schema field as a clean labeled row.
//   * Generated timestamp.
//   * Footer note: "Generated via Scriba — verified by patient/family."
// - Accept a validated PatientIntake object as prop.
// - NOTE: rendered inside /api/generate-pdf (server); @react-pdf/renderer is
//   marked as an external package in next.config.mjs.
export {}; // scaffold — implemented in Step 5
