// patient.ts — TypeScript types generated from / matching lib/schema.ts
//
// TODO(Step 3):
// - Once lib/schema.ts defines patientIntakeSchema, re-export the inferred
//   types here so UI code never hand-writes patient shapes:
//     import { z } from "zod";
//     import { patientIntakeSchema } from "@/lib/schema";
//     export type PatientIntake = z.output<typeof patientIntakeSchema>;
//     export type PatientIntakeInput = z.input<typeof patientIntakeSchema>;
// - Also export the ChatMessage type used by the intake screen + /api/chat:
//     { role: "user" | "assistant"; content: string }
export {}; // scaffold — implemented in Step 3
